<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="XCVTransformer.Pages.TranslatorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XCVTransformer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="0 0 0 1" BorderBrush="{StaticResource LineDetail}"  CornerRadius="10">
            <TextBlock Text="Traducir" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 10 0 10"/>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" BorderThickness="0 0 1 0" BorderBrush="{StaticResource LineDetail}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Idioma Origen" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <ListView x:Name="OrigenList" Grid.Row="1" CornerRadius="{StaticResource ControlCornerRadius}"
                          BorderThickness="1"
                          BorderBrush="{StaticResource LineDetail}"
                          HorizontalAlignment="Center"
                          ItemsSource="{Binding Words}"
                          SelectedItem="{Binding SelectedOriginLanguage, Mode=TwoWay}">
                    <!-- Enlazando la propiedad Words -->

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Text="{Binding}" />
                            <!-- Cada palabra será un TextBlock -->
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Idioma destino" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <ListView x:Name="EndList" Grid.Row="1" CornerRadius="{StaticResource ControlCornerRadius}"
                          BorderThickness="1"
                          BorderBrush="{StaticResource LineDetail}"
                          HorizontalAlignment="Center"
                          ItemsSource="{Binding Words}"
                          SelectedItem="{Binding SelectedEndLanguage, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

        </Grid>
    </Grid>
</Page>
